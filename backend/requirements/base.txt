Django==5.1.4
djangorestframework==3.15.2
djangorestframework-gis==1.1
django-filter==24.3
django-environ==0.11.2
django-cors-headers==4.6.0
psycopg[binary]==3.2.3
requests==2.32.3
tqdm==4.67.1

# Faz 2 — canlı veri adaptörü: Celery worker + beat, Redis broker
celery>=5.4,<6
redis>=5.0,<6
django-celery-beat>=2.6,<3

# Faz 2 Step 2 — schema validation between adapters and pipeline layers
pydantic>=2.7,<3

# Faz 2 Step 5i-iii — Turkish public holiday calendar (refresh task picks
# Sunday timetable as fallback on holidays per İBB scheduling convention)
holidays>=0.50,<1

# Faz 3 — WebSocket katmanı: Channels (ASGI consumers + group fanout)
# + Daphne (production ASGI server) + Redis-backed channel layer.
channels[daphne]>=4.1,<5
channels-redis>=4.2,<5

# Faz 3 6h-i — vehicle-to-route spatial sanity check (vectorized
# haversine min distance over GTFS shape pool).
numpy>=2.0,<3
